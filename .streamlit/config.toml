# Pinned light theme. Soft-gray canvas so the white cards/panels lift off the
# page with subtle shadows (a flat white-on-white looked cramped).
[theme]
base = "light"
primaryColor = "#10b981"
backgroundColor = "#f3f5f9"
secondaryBackgroundColor = "#ffffff"
textColor = "#0f172a"
